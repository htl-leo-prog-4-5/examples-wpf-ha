<Window x:Class="Survey.Wpf.Views.ShowPollResultSummaryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
         xmlns:vm="clr-namespace:Survey.Wpf.ViewModels"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Survey.Wpf.Views"
        mc:Ignorable="d"
        Title="Poll Result Summary" Height="300" Width="300">
    
    <Window.DataContext>
        <vm:ShowPollResultSummaryViewModel />
    </Window.DataContext>

    <StackPanel>
        <Button Command="{Binding LoadCommand}" Content="Start"  Margin="0,0,5,0"/>
        <DataGrid Margin="0,10,0,0" MinHeight="200" ItemsSource="{Binding Results}"
                	AutoGenerateColumns="False" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Visible" 
                	ScrollViewer.HorizontalScrollBarVisibility="Auto" CanUserAddRows="False" IsReadOnly="True" MaxHeight="600"  >

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Answer}" Header="Antwort" MinWidth="100" IsReadOnly="True"/>
                <DataGridTextColumn Binding="{Binding Count}" Header="Anzahl" MinWidth="100" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <TextBox Text="{Binding Poll.Name}"  IsReadOnly="True" Margin="0,0,5,0"/>
    </StackPanel>
</Window>
