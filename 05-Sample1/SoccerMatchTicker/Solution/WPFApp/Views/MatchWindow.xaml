<Window x:Class="WPFApp.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        xmlns:viewModels="clr-namespace:WPFApp.ViewModels"
        Title="Soccer Match Ticker" Height="500" Width="700">

    <Window.DataContext>
        <viewModels:MatchViewModel />
    </Window.DataContext>

    <DockPanel>
        <StackPanel Orientation="Vertical" DockPanel.Dock="Top" Margin="0,5,0,0">
            <StackPanel Orientation="Horizontal">
                <Label Content="Filename" Width="100" />
                <TextBox Text="{Binding FileName}" MinWidth="300" />
                <Button Content="Load" Command="{Binding LoadCommand}" Width="60" Margin="5,0,0,0" />
            </StackPanel>
            <Separator Margin="0,5,0,5" />
            <StackPanel Orientation="Horizontal">
                <Label Content="Name of team 1" Width="100" />
                <TextBox Text="{Binding Match.TeamName1}" MinWidth="400" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Name of team 2" Width="100" />
                <TextBox Text="{Binding Match.TeamName2}" MinWidth="400" />
            </StackPanel>

        </StackPanel>

        <StackPanel Orientation="Vertical" DockPanel.Dock="Bottom" Margin="0,5,0,5">
            <StackPanel Orientation="Horizontal">
                <Label Content="Time" Width="100" />
                <TextBox Text="{Binding Match.NewEventTime, UpdateSourceTrigger=PropertyChanged}" MinWidth="50" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Event" Width="100" />
                <TextBox Text="{Binding Match.NewEventText, UpdateSourceTrigger=PropertyChanged}" MinWidth="100" />
            </StackPanel>
            <StackPanel Orientation="Horizontal">
                <Label Content="Information" Width="100" />
                <TextBox Text="{Binding Match.NewEventInfo, UpdateSourceTrigger=PropertyChanged}" MinWidth="200" />
                <Button Content="Add new event" Command="{Binding SaveCommand}" Margin="20,0,0,0" />
            </StackPanel>
        </StackPanel>

        <DataGrid Margin="0,10,0,0" MinHeight="100" ItemsSource="{Binding Match.Events}"
                  AutoGenerateColumns="False" ScrollViewer.CanContentScroll="True"
                  ScrollViewer.VerticalScrollBarVisibility="Visible"
                  ScrollViewer.HorizontalScrollBarVisibility="Auto" CanUserAddRows="False" IsReadOnly="True"
                  MaxHeight="600">

            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding EventTime}" Header="Time" MinWidth="100" IsReadOnly="True" />
                <DataGridTextColumn Binding="{Binding EventText}" Header="Event" MinWidth="50" IsReadOnly="True" />
                <DataGridTextColumn Binding="{Binding EventInfo}" Header="Information" MinWidth="250" IsReadOnly="True" />
            </DataGrid.Columns>
        </DataGrid>

    </DockPanel>
</Window>