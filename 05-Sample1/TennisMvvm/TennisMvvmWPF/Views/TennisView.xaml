<Window x:Class="TennisMvvmWPF.Views.PlayerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TennisMvvmWPF.Views"
        mc:Ignorable="d"
        Title="Tennis Spieler" Height="300" Width="300"
        xmlns:vm="clr-namespace:TennisMvvmWPF.ViewModels"        
        >

    <Window.DataContext>
        <vm:PlayerViewModel />
    </Window.DataContext>

    <StackPanel Orientation="Vertical">
        <Label Content="Tennis" FontSize="20" />

        <Grid Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label Content="Name" Grid.Row="0" Grid.Column="0"></Label>
            <TextBox Text="{Binding PlayerName}" Grid.Row="0" Grid.Column="1"></TextBox>

            <Label Content="Points" Grid.Row="1" Grid.Column="0"></Label>
            <TextBox Text="{Binding Points}" Grid.Row="1" Grid.Column="1"></TextBox>

        </Grid>
        <DataGrid MinHeight="100" ItemsSource="{Binding Results}"
                	AutoGenerateColumns="False" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Visible" 
                	ScrollViewer.HorizontalScrollBarVisibility="Auto" CanUserAddRows="True" IsReadOnly="False" MaxHeight="600"  >

            <DataGrid.Columns>
            <DataGridTextColumn Binding="{Binding City}" Header="Stadt" MinWidth="100" IsReadOnly="False"/>
            <DataGridTextColumn Binding="{Binding Rank}" Header="Rank" MinWidth="100" IsReadOnly="False"/>
        </DataGrid.Columns>
        </DataGrid>

        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" >
            <Button Content="Load" Command="{Binding LoadCommand}" Width="50" />
            <Button Content="Save" Command="{Binding SaveCommand}" Width="50" Margin="5,0,0,0"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" >
            <Label Content="Filename" ></Label>
            <TextBox Text="{Binding FileName}"></TextBox>
        </StackPanel>
    </StackPanel>
</Window>
