<Window x:Class="F1FahrerMvvmWPF.Views.F1Driver"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:F1FahrerMvvmWPF.ViewModels"  
        mc:Ignorable="d"
        Title="F1Driver" Height="300" Width="300"
        >
    <Window.DataContext>
        <vm:F1DriverViewModel/>
    </Window.DataContext>


    <StackPanel Orientation="Vertical">
        <Label Content="F1 Driver" FontSize="20" />

        <Grid Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label Content="Name" Grid.Row="0" Grid.Column="0"></Label>
            <TextBox Text="{Binding DriverName}" Grid.Row="0" Grid.Column="1"></TextBox>

            <Label Content="Points" Grid.Row="1" Grid.Column="0"></Label>
            <TextBox Text="{Binding Points}" Grid.Row="1" Grid.Column="1"></TextBox>

        </Grid>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0">
            <Button Content="Load" Command="{Binding LoadCommand}" Width="50" />
            <Button Content="Save" Command="{Binding SaveCommand}" Width="50" Margin="5,0,0,0" />
            <Button Content="Win" Command="{Binding WinCommand}" Width="50" Margin="5,0,0,0" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0">
            <Label Content="Filename"></Label>
            <TextBox Text="{Binding FileName}"></TextBox>
        </StackPanel>
    </StackPanel>
</Window>