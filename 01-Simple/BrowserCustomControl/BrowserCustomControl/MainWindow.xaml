<Window x:Class="BrowserCustomControl.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BrowserCustomControl"
        mc:Ignorable="d"
        Title="MainWindow" Height="200" Width="525">
    <Window.DataContext>
        <local:MainWindowViewModel />
    </Window.DataContext>
    <StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="File browser" Width="200"/>
            <local:BrowserEditControl Width="200" MyContent="{Binding Path=FileName}" BrowseCommand="{Binding SelectFileNameCommand}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Clear" Width="200"/>
            <local:BrowserEditControl Width="200" MyContent="{Binding FileName}" BrowseCommand="{Binding ClearFilenameCommand}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Label Content="Set filename" Width="200"/>
            <local:BrowserEditControl Width="200" MyContent="{Binding FileName}" BrowseCommand="{Binding SetFilenameCommand}"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal">

        </StackPanel>

    </StackPanel>
</Window>
