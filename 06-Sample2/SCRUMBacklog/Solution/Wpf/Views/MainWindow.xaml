<Window x:Class="Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf"
        xmlns:viewModels="clr-namespace:Wpf.ViewModels;assembly=Wpf.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="SCRUM Backlog" Height="460" Width="620">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Filterbereich -->
        <StackPanel Margin="10" Grid.Row="0" Orientation="Horizontal">
            <Label Content="Team Member:" />
            <ComboBox Width="150" Margin="5" ItemsSource="{Binding TeamMembers}" DisplayMemberPath="Name"
                      SelectedValue="{Binding SelectedTeamMember}" />

            <Button Content="Filter" Width="60" Margin="10,0,0,0" Command="{Binding FilterCommand}" />
        </StackPanel>

        <ListView Grid.Row="1" Margin="10"
                  ItemsSource="{Binding FilteredBacklogItems}" SelectedItem="{Binding SelectedBacklogItem}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="CreationDate" DisplayMemberBinding="{Binding CreationDate}" />
                    <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                    <GridViewColumn Header="Effort" DisplayMemberBinding="{Binding Effort}" />
                    <GridViewColumn Header="Priority" DisplayMemberBinding="{Binding Priority}" />
                    <GridViewColumn Header="TeamMembers" DisplayMemberBinding="{Binding TeamMembers}" />
                    <GridViewColumn Header="Comments" DisplayMemberBinding="{Binding Comments}" />
                </GridView>
            </ListView.View>
        </ListView>
        <UniformGrid Grid.Row="2" Columns="2" Margin="10">

            <Label Content="Priority:" />
            <TextBox Text="{Binding Priority, TargetNullValue='', UpdateSourceTrigger=PropertyChanged}"
                     IsReadOnly="{Binding IsNotEditMode}" />

        </UniformGrid>

        <UniformGrid Grid.Row="3" Columns="4" Margin="5">
            <Button Content="Details" Margin="5" Command="{Binding DetailCommand}" />
            <Button Content="Edit" Margin="5" Command="{Binding EditCommand}" />
            <Button Content="Update" Margin="5" Command="{Binding UpdateCommand}" />
        </UniformGrid>
    </Grid>
</Window>