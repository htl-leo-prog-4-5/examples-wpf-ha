<Application x:Class="WinUIWpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WinUIWpf"
             Startup="AppStartup">

    <Application.Resources>
        <Style TargetType="TextBlock" x:Key="Header">
            <Setter Property="FontSize" Value="30" />
            <Setter Property="Padding" Value="10,5,10,10" />
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
        </Style>
        <Style TargetType="TextBlock" x:Key="HeaderSubtitle" BasedOn="{StaticResource Header}">
            <Setter Property="FontSize" Value="15" />
            <Setter Property="HorizontalAlignment" Value="Left" />
        </Style>
        <Style TargetType="Button" x:Key="ActionButton">
            <Setter Property="FontSize" Value="14"></Setter>
            <Setter Property="Height" Value="30"></Setter>
            <Setter Property="Margin" Value="10,5,10,10" />
            <Setter Property="Padding" Value="5"></Setter>
        </Style>
        <Style TargetType="Button" x:Key="DeleteButton" BasedOn="{StaticResource ActionButton}">
            <Setter Property="Background" Value="Red"></Setter>
        </Style>

        <Style x:Key="GridColumn" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12"></Setter>
            <Setter Property="Padding" Value="5"></Setter>
        </Style>
        <Style x:Key="StringColumn" TargetType="TextBlock" BasedOn="{StaticResource GridColumn}">
            <Setter Property="HorizontalAlignment" Value="Left"></Setter>
        </Style>
        <Style x:Key="NumberColumn" TargetType="TextBlock" BasedOn="{StaticResource GridColumn}">
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
        </Style>
        <Style x:Key="DateColumn" TargetType="TextBlock" BasedOn="{StaticResource GridColumn}">
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
        </Style>
        <Style x:Key="TextColumn" TargetType="TextBlock" BasedOn="{StaticResource GridColumn}">
            <Setter Property="HorizontalAlignment" Value="Left"></Setter>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
        </Style>
        <Style x:Key="CheckboxColumn" TargetType="CheckBox">
            <Setter Property="Padding" Value="5"></Setter>
            <Setter Property="HorizontalAlignment" Value="Center"></Setter>
            <Setter Property="VerticalAlignment" Value="Center"></Setter>
        </Style>

    </Application.Resources>
</Application>