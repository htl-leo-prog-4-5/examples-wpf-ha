<Window x:Class="Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf"
        xmlns:viewModels="clr-namespace:Wpf.ViewModels;assembly=Wpf.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel}"
        mc:Ignorable="d"
        Title="Travel Agency" Height="460" Width="620">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Filterbereich -->
        <StackPanel Margin="10" Grid.Row="0" Orientation="Horizontal">
            <Label Content="From date:" />
            <DatePicker Width="150" Margin="5" SelectedDate="{Binding DateFrom , Mode=TwoWay}" />

            <Label Content="To date:" Margin="10,0,0,0" />
            <DatePicker Width="150" Margin="5" SelectedDate="{Binding DateTo, Mode=TwoWay}" />

            <Button Content="Filter" Width="60" Margin="10,0,0,0" Command="{Binding FilterCommand}" />
        </StackPanel>

        <ListView Grid.Row="1" Margin="10"
                  ItemsSource="{Binding Filtered}" SelectedItem="{Binding SelectedTrip}">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Route"
                                    DisplayMemberBinding="{Binding Route.Name}" />
                    <GridViewColumn Header="Tip from date"
                                    DisplayMemberBinding="{Binding DepartureDateTime, StringFormat={}{0:yyyy.MM.dd}}" />
                    <GridViewColumn Header="Tip to date"
                                    DisplayMemberBinding="{Binding ArrivalDateTime, StringFormat={}{0:yyyy.MM.dd}}" />
                </GridView>
            </ListView.View>
        </ListView>

        <UniformGrid Grid.Row="2" Columns="4">
            <Button Content="Details" Margin="10" Command="{Binding DetailCommand}" />
            <Button Content="New" Margin="10" Command="{Binding NewCommand}" />
        </UniformGrid>
    </Grid>
</Window>