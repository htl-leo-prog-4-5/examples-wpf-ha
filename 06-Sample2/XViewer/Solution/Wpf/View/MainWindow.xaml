<Window x:Class="Wpf.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:Wpf.Controls"
        xmlns:viewModels="clr-namespace:Wpf.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
        mc:Ignorable="d"
        Title="X Viewer" Height="450" Width="600">
    <DockPanel>
        <TextBlock DockPanel.Dock="Top" FontSize="26">X Viewer</TextBlock>
        <DockPanel>

            <StackPanel DockPanel.Dock="Left">
                <ListView Margin="5" ItemsSource="{Binding Drawings}" SelectedItem="{Binding SelectedDrawing}">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" />
                        </GridView>
                    </ListView.View>
                </ListView>
                <Button Margin="5" Command="{Binding LoadNewDrawingFileCommand}">Load drawing file...</Button>
                <Button Margin="5" Command="{Binding DeleteDrawingCommand}">Delete</Button>

            </StackPanel>
            <controls:DrawingControl Margin="10,0,10,10" BorderThickness="1" BorderBrush="Black"
                                     Drawing="{Binding SelectedDrawing}" />

        </DockPanel>
    </DockPanel>
</Window>