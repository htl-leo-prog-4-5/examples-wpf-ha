<Window x:Class="WpfTadeotAdmin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        d:DataContext="{d:DesignInstance Type=viewModels:MainViewModel}"
        xmlns:local="clr-namespace:WpfTadeotAdmin"
        xmlns:viewModels="clr-namespace:WpfTadeotAdmin.ViewModels;assembly=WpfTadeotAdmin.ViewModels"
        mc:Ignorable="d"
        Title="Tadeot Admin Panel" Height="600" Width="1100" Loaded="Window_Loaded">

    <Window.Resources>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="50"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <UniformGrid Grid.Row="0" Columns="2" HorizontalAlignment="Stretch" Margin="10">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                <Image Source="/img/htllogo_2022_black.png" Width="200" Stretch="Uniform" />
            </StackPanel>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <TextBlock Style="{StaticResource Header}">Tadeot Administration</TextBlock>
            </StackPanel>
        </UniformGrid>

        <TextBlock Grid.Row="1" Margin="10" FontSize="24"
                   Text="{Binding Path=VisitorsCount, StringFormat='Besucherregistrierungen: {0}'}">
        </TextBlock>
        <DataGrid Margin="10" Grid.Row="2" AutoGenerateColumns="False">
            <DataGrid.Columns>
            </DataGrid.Columns>
        </DataGrid>

        <UniformGrid Grid.Row="3" Columns="4">
            <Button>ToDo</Button>
        </UniformGrid>


    </Grid>
</Window>