<Window x:Class="WpfTadeotAdmin.Views.RegistrationConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTadeotAdmin.Views"
        xmlns:viewModels="clr-namespace:WpfTadeotAdmin.ViewModels;assembly=WpfTadeotAdmin.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewModels:RegistrationConfigViewModel}"
        mc:Ignorable="d"
        Title="Tadeot Admin - Config Registration" Height="450" Width="800" Loaded="Window_Loaded">
    <Window.Resources>
        <Style TargetType="TextBox" x:Key="TextList">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Margin" Value="10,5,10,10" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="VerticalScrollBarVisibility" Value="Visible" />
        </Style>
    </Window.Resources>
    <Grid>
    </Grid>
</Window>