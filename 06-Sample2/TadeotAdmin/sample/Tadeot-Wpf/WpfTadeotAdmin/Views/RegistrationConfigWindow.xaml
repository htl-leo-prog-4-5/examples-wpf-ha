<Window x:Class="WpfTadeotAdmin.Views.RegistrationConfigWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfTadeotAdmin.Views"
        mc:Ignorable="d"
        Title="Tadeot Admin - Config Registration" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBox" x:Key="TextList">
            <Setter Property="FontSize" Value="11" />
            <Setter Property="Margin" Value="10,5,10,10" />
            <Setter Property="TextWrapping" Value="Wrap" />
            <Setter Property="AcceptsReturn" Value="True" />
            <Setter Property="VerticalScrollBarVisibility" Value="Visible"/>
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Margin="10" Text="Konfiguration Registrierungs-Auswahllisten" FontSize="36"></TextBlock>
        <UniformGrid Grid.Row="1" Columns="2">
            <StackPanel Grid.Column="0" Orientation="Vertical">
                <TextBlock Style="{StaticResource HeaderSubtitle}">Auswahlliste 'Gründe für Besuch'</TextBlock>
                <TextBox Style="{StaticResource TextList}" Text="{Binding Path=ReasonsText, UpdateSourceTrigger=PropertyChanged}" ></TextBox>
                <Button Style="{StaticResource ActionButton}" Command="{Binding UpdateReasons}">Neue Gründe speichern</Button>
            </StackPanel>
            <StackPanel Orientation="Vertical">
                <TextBlock Style="{StaticResource HeaderSubtitle}">Auswahlliste 'Schultypen'</TextBlock>
                <TextBox Style="{StaticResource TextList}" Text="{Binding Path=TypesText, UpdateSourceTrigger=PropertyChanged}" ></TextBox>
                <Button Style="{StaticResource ActionButton}" Command="{Binding UpdateTypes}">Neue Schultypen speichern</Button>

            </StackPanel>
        </UniformGrid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left">
            <Button Command="{Binding CloseWindow}" Style="{StaticResource ActionButton}">Schließen</Button>
        </StackPanel>
    </Grid>
</Window>
