services:
  seq:
    image: datalust/seq:latest
    container_name: seq
    volumes:
      - seq-data:/data          
    restart: unless-stopped
    environment:
      ACCEPT_EULA: "Y"
    ports:
      - 5341:80
volumes:
  seq-data: