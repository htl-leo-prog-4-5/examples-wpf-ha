<Window x:Class="WinUIWpf.Views.AddLockerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinUIWpf.Views" xmlns:viewmodels="clr-namespace:WinUIWpf.ViewModels"
        d:DataContext="{d:DesignInstance Type=viewmodels:AddLockerWindowViewModel}"
        mc:Ignorable="d"
        Title="Spind Hinzufügen" Height="180" Width="400">

    <Window.Resources>
        <Style TargetType="Label" x:Key="ErrorFontColorStyle">
            <Setter Property="Foreground" Value="Red" />
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="2*" />
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Spindnummer: " Margin="10" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding NumberText, UpdateSourceTrigger=PropertyChanged}"
                 MinWidth="80" Margin="10" VerticalContentAlignment="Center">
        </TextBox>
        <Label Grid.Row="1" Grid.ColumnSpan="2" Margin="10 0 0 10" Style="{StaticResource ErrorFontColorStyle}"
               Content="{Binding ValidationError}">
        </Label>
        <UniformGrid Grid.Row="2" Grid.ColumnSpan="2" Columns="1">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Speichern" Margin="10" MaxHeight="25"
                        Command="{Binding SaveLockerCommand}"
                        Width="100" HorizontalAlignment="Right" />
            </StackPanel>
        </UniformGrid>

    </Grid>
</Window>