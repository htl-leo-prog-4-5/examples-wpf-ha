<Window x:Class="NotenschnittMvvmWPF.Views.SchuelerView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NotenschnittMvvmWPF.Views"
        xmlns:viewModels="clr-namespace:NotenschnittMvvmWPF.ViewModels"
        mc:Ignorable="d"
        Title="Schueler" Height="300" Width="300">

    <Window.DataContext>
        <viewModels:SchuelerViewModel />
    </Window.DataContext>

    <StackPanel Orientation="Vertical">
        <Label Content="Notenschnitt" FontSize="20" />

        <Grid Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="30" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Label Content="Name" Grid.Row="0" Grid.Column="0"></Label>
            <TextBox Text="{Binding SchuelerName}" Grid.Row="0" Grid.Column="1"></TextBox>

            <Label Content="Math" Grid.Row="1" Grid.Column="0"></Label>
            <TextBox Text="{Binding Mathematik}" Grid.Row="1" Grid.Column="1"></TextBox>

            <Label Content="Deutsch" Grid.Row="2" Grid.Column="0"></Label>
            <TextBox Text="{Binding Deutsch}" Grid.Row="2" Grid.Column="1"></TextBox>

            <Label Content="Englisch" Grid.Row="3" Grid.Column="0"></Label>
            <TextBox Text="{Binding Englisch}" Grid.Row="3" Grid.Column="1"></TextBox>

            <Label Content="Notenschnitt" Grid.Row="4" Grid.Column="0"></Label>
            <TextBox Text="{Binding Schnitt}" Grid.Row="4" Grid.Column="1"></TextBox>

        </Grid>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" >
            <Button Content="Load" Command="{Binding LoadCommand}" Width="50" />
            <Button Content="Save" Command="{Binding SaveCommand}" Width="50" Margin="5,0,0,0"/>
            <Button Content="Berechnen" Command="{Binding CalcCommand}" Width="75" Margin="5,0,0,0" />
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="2" Grid.Column="0" >
            <Label Content="Filename" ></Label>
            <TextBox Text="{Binding FileName, UpdateSourceTrigger=PropertyChanged}"></TextBox>
            <Button Content="?" ></Button>
        </StackPanel>
    </StackPanel>
</Window>
