<Window x:Class="Enter6Aus45.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Enter6Aus45"
        xmlns:vm="clr-namespace:Enter6Aus45.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainWindowViewModel />
    </Window.DataContext>
    <StackPanel>
        <Label>6 aus 45</Label>
        <Separator></Separator>
        <StackPanel Orientation="Horizontal">
            <Label Content="File" Width="100" />
            <TextBox Text="{Binding FileName, UpdateSourceTrigger=PropertyChanged}" />
        </StackPanel>
        <StackPanel Orientation="Horizontal">
            <Button Content="Create Empty File" Command="{Binding CreateEmptyCommand}" />
            <Button Content="Add Tip" Command="{Binding AddTipCommand}" Margin="10,0,0,0"/>
        </StackPanel>

        <TextBox  Text="{Binding ResultText}"/>

    </StackPanel>
</Window>
